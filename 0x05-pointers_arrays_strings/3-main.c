#include "main.h"

/**
 * main - entry point
 * @str: pointer of char type to print
 *
 * main - a function that prints a string, followed by a new line, to stdout.
 * Return: Always 0.
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimo v";
	_puts(str);
	return (0);
}
