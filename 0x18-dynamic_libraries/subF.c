#include "myh.h"
#include <stdio.h>

/**
 * sub - subtract integers
 * @a: integer to input
 * @b: integer to input
 *
 * sub - subtract two integers
 * Return: diff
 */

int sub(int a, int b)
{
	int diff;

	diff = a - b;
	return (diff);
}
