#include "myh.h"
#include <stdio.h>

/**
 * div - divides integers
 * @a: integer to imput
 * @b:integer to input
 *
 * div - divides two integers
 * Return: qout.
 */

int div(int a, int b)
{
	int qout;

	qout = a / b;
	return (qout);
}
