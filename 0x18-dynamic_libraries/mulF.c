#include "myh.h"
#include <stdio.h>

/**
 * mul - multiplies integers
 * @a: integer to input
 * @b: integer to input
 *
 * mul - multiplies two integer
 * Return: prod.
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
