#include "myh.h"
#include <stdio.h>

/**
 * mod - finds the reminder
 * @a: integer to input
 * @b: integer to input
 *
 * mod - finds the reminder of two integers
 * Return: rem.
 */

int mod(int a, int b)
{
	int rem;

	rem = a % b;
	return (rem);
}
