#include "myh.h"
#include <stdio.h>

/**
 * add - adds integers
 * @a: integer to input
 * @b: integer to input
 *
 * add - adds two integers
 * Return: sum.
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
